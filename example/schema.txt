CREATE TABLE users (
  id INT PRIMARY KEY,
  email TEXT,
  created_at TIMESTAMP
);

CREATE TABLE orders (
  id INT PRIMARY KEY,
  user_id INT,
  total DECIMAL,
  created_at TIMESTAMP
);

ALTER TABLE orders
ADD CONSTRAINT fk_orders_users FOREIGN KEY (user_id) REFERENCES users(id);